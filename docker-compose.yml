services:
  telegram-log-bot:
    build: .
    container_name: telegram-log-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config/services.yml:/app/config/services.yml:ro
